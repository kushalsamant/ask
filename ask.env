# =============================================================================
# ASK: Daily Architectural Research - Configuration
# =============================================================================

# API CONFIGURATION
# =============================================================================
# TOGETHER_API_KEY=tgp_v1_wPKrrFO2BL-U5lK6viBkrtLoWs4MD9_TPgE0RBC5Wgs        # API key
TOGETHER_API_BASE=https://api.together.xyz/v1  # API base URL

# AI MODEL CONFIGURATION
# =============================================================================
TEXT_MODEL=meta-llama/Llama-3.3-70B-Instruct-Turbo-Free  # Text model
IMAGE_MODEL=black-forest-labs/FLUX.1-schnell-Free  # Image model
VISION_MODEL=meta-llama/Llama-Vision-Free  # Vision model

# API TIMEOUT AND RATE LIMITING
# =============================================================================
API_TIMEOUT=60                    # Request timeout (seconds)
RATE_LIMIT_DELAY=10.0            # Delay between requests (seconds)
API_MAX_RETRIES=5                # Max retry attempts (for 5 delays: 30,60,90,120,150)
API_RETRY_DELAY=30               # Initial retry delay (seconds)
API_EXPONENTIAL_BACKOFF=false    # Use fixed retry delays instead of exponential
DOWNLOAD_TIMEOUT=30              # Download timeout (seconds)
MAX_RETRIES=5                    # Max image generation retries
RETRYABLE_STATUS_CODES=408,429,500,502,503,504  # Retry status codes
RETRY_DELAY_MULTIPLIER=30        # Retry delay increment (30s between each retry)

# TEXT GENERATION CONFIGURATION
# =============================================================================
TEXT_TEMPERATURE=0.72            # Temperature (0.0-1.0)
TEXT_MAX_TOKENS=150              # Max tokens
TEXT_TOP_P=0.85                  # Top-p sampling
TEXT_FREQUENCY_PENALTY=0.3       # Frequency penalty
TEXT_PRESENCE_PENALTY=0.6        # Presence penalty

# IMAGE GENERATION CONFIGURATION
# =============================================================================
IMAGE_WIDTH=1072                 # Image width
IMAGE_HEIGHT=1792                # Image height
IMAGE_QUALITY=95                 # JPEG quality
IMAGE_STYLE=photographic         # Image style
IMAGE_ASPECT_RATIO=9:16          # Aspect ratio
INFERENCE_STEPS=4                # Inference steps
GUIDANCE_SCALE=7.5               # Guidance scale

# IMAGE GENERATION FEATURES (Converted from PDF Generation)
# =============================================================================
CREATE_INDIVIDUAL_IMAGES=true                    # Create individual images for each Q&A pair
CREATE_FINAL_COMPILATION=true                    # Create final compilation image
CREATE_AUTOMATIC_CATEGORY_COMPILATIONS=false     # Create automatic category compilations
CREATE_COVER_IMAGE=true                          # Create cover image for volume
CREATE_TABLE_OF_CONTENTS=true                    # Create table of contents image
CREATE_SEQUENTIAL_TOC=true                       # Create sequential table of contents
CREATE_CATEGORY_TOC=true                         # Create category table of contents
PRESERVE_TEMP_FILES=true                         # Preserve temporary files

# TABLE OF CONTENTS FEATURES (Converted from PDF TOC)
# =============================================================================
TOC_SHOW_FULL_QUESTIONS=true                     # Show full questions in TOC
TOC_BACKGROUND_PROMPT=true                       # Use background prompt for TOC images
TOC_CATEGORY_GROUPING=true                       # Group by category in TOC
TOC_GROUP_UNKNOWN_CATEGORIES=true                # Group unknown categories
TOC_SORT_CATEGORIES_ALPHABETICALLY=true          # Sort categories alphabetically
TOC_UNKNOWN_CATEGORY_NAME=Uncategorized          # Unknown category name
TOC_IMAGE_NUMBER_SPACE=50                        # Image number space
TOC_QUESTION_PREVIEW_LENGTH=0                    # Question preview length

# TOC TEMPLATES (Converted from PDF TOC templates)
# =============================================================================
TOC_MAIN_TITLE_TEMPLATE=ASK: Daily Architectural Research - Volume {volume_number}  # Main title
TOC_SEQUENTIAL_SUBTITLE=Sequential Table of Contents  # Sequential subtitle
TOC_CATEGORY_SUBTITLE=Category Index  # Category subtitle
TOC_SEQUENTIAL_SECTION_TITLE=Sequential Order  # Sequential section
TOC_CATEGORY_INFO_TEMPLATE={count} Q&A Pairs • Sequential Order  # Category info
TOC_TOTAL_COUNT_TEMPLATE=Total Q&A Pairs: {count}  # Total count
TOC_CATEGORY_OVERVIEW_TEMPLATE=Category Overview and Research Insights  # Category overview
TOC_RESEARCH_INSIGHTS_TEMPLATE=Research Insights: {insights}...  # Research insights
TOC_DATE_TEMPLATE=Generated: {date}  # Date template
TOC_TITLE_TEMPLATE=Table of Contents  # TOC title
TOC_SUBTITLE_TEMPLATE=ASK: Daily Architectural Research - Volume {volume_number}  # TOC subtitle
TOC_IMAGE_NUMBER_TEMPLATE={image_number}  # Image number template
TOC_SUMMARY_TEMPLATE=Total Q&A Pairs: {qa_count} | Categories: {category_count}  # Summary

# INDIVIDUAL IMAGE SETTINGS (Converted from PDF Individual)
# =============================================================================
INDIVIDUAL_FILENAME_TEMPLATE=ASK_{image_number}_{category}.jpg  # Individual filename
INDIVIDUAL_FALLBACK_TEMPLATE=ASK_QA_{category}.jpg  # Fallback filename
FOOTER_BRAND_TEXT=ASK: Daily Architectural Research  # Footer brand
FOOTER_IMAGE_NUMBER_TEMPLATE=ASK-{image_number}  # Footer image number

# PAGE AND LAYOUT SETTINGS (Converted from PDF Page)
# =============================================================================
PAGE_WIDTH=1072              # Page width
PAGE_HEIGHT=1792             # Page height
PAGE_BREAK_BUFFER=100        # Page break buffer
MAX_TEXT_LINES=8             # Max text lines
TEXT_WRAP_WIDTH_ANSWER=60     # Answer wrap width
TEXT_WRAP_WIDTH_OVERLAY=50   # Overlay wrap width
FOOTER_HEIGHT=80             # Footer height
FOOTER_BRAND_X=40            # Footer brand X
FOOTER_BRAND_Y_OFFSET=20     # Footer brand Y offset
FOOTER_CATEGORY_Y_OFFSET=20  # Footer category Y offset
FOOTER_IMAGE_X_OFFSET=40     # Footer image X offset
FOOTER_IMAGE_Y_OFFSET=20     # Footer image Y offset
MARGIN_BOTTOM=200            # Bottom margin

# TYPOGRAPHY SETTINGS (Converted from PDF Typography)
# =============================================================================
FONT_FAMILY_PRIMARY=Helvetica      # Primary font
FONT_FAMILY_SECONDARY=Times-Roman  # Secondary font
FONT_FAMILY_MONOSPACE=Courier      # Monospace font
FONT_SIZE_TITLE=36                 # Title size
FONT_SIZE_SUBTITLE=18              # Subtitle size
FONT_SIZE_SECTION=16               # Section size
FONT_SIZE_CATEGORY_HEADER=20       # Category header size
FONT_SIZE_ENTRY=14                 # Entry size
FONT_SIZE_QUESTION=12              # Question size
FONT_SIZE_DETAIL=10                # Detail size
FONT_SIZE_FOOTER=14                # Footer size
FONT_SIZE_CAPTION=8                # Caption size
FONT_SIZE_COVER_TITLE=48           # Cover title size
FONT_SIZE_COVER_SUBTITLE=24        # Cover subtitle size
FONT_SIZE_COVER_BRAND=18           # Cover brand size

# COLOR SETTINGS (Converted from PDF Colors)
# =============================================================================
COLOR_PRIMARY=#000000              # Primary color
COLOR_SECONDARY=#2C3E50            # Secondary color
COLOR_ACCENT=#E74C3C               # Accent color
COLOR_CATEGORY_HEADER=#8E44AD      # Category header color
COLOR_MUTED=#7F8C8D                # Muted color
COLOR_BRAND=#34495E                # Brand color
FOOTER_BACKGROUND_COLOR=0.1,0.1,0.1  # Footer background

# SPACING SETTINGS (Converted from PDF Spacing)
# =============================================================================
LINE_SPACING_SINGLE=1.0            # Single spacing
LINE_SPACING_ONE_HALF=1.5          # 1.5 spacing
LINE_SPACING_DOUBLE=2.0            # Double spacing
CHAR_SPACING_NORMAL=0              # Normal character spacing
CHAR_SPACING_TIGHT=-0.5            # Tight character spacing
CHAR_SPACING_WIDE=1.0              # Wide character spacing
TITLE_TO_SUBTITLE_SPACING=20       # Title to subtitle spacing
SUBTITLE_TO_SECTION_SPACING=30     # Subtitle to section spacing
SECTION_TO_CONTENT_SPACING=40      # Section to content spacing
ENTRY_SPACING=15                   # Entry spacing
CATEGORY_SPACING=20                # Category spacing
SECTION_SPACING=25                 # Section spacing
LINE_SPACING=30                    # Line spacing

# MARGIN SETTINGS (Converted from PDF Margins)
# =============================================================================
MARGIN_LEFT=72                     # Left margin
MARGIN_TOP=72                      # Top margin
MARGIN_RIGHT=72                    # Right margin
MARGIN_BOTTOM=72                   # Bottom margin

# COVER GENERATION SETTINGS (Converted from PDF Cover)
# =============================================================================
COVER_PROMPT=Professional architectural research compilation cover with modern design elements, typography, and visual hierarchy  # Cover prompt
COVER_TITLE=ASK: Daily Architectural Research  # Cover title
COVER_TEXT_COLOR=#000000           # Cover text color
COVER_BRAND_COLOR=#2C3E50          # Cover brand color

# LOGGING FEATURES (Converted from PDF Logging)
# =============================================================================
LOG_SUCCESS_MESSAGES=true          # Log success messages
LOG_ERROR_MESSAGES=true            # Log error messages
LOG_PROGRESS_MESSAGES=true         # Log progress messages
LOG_DETAILED_ERRORS=true           # Log detailed errors
LOG_TIMING=true                    # Log timing information
LOG_LEVEL=INFO                     # Log level

# PROGRESS TRACKING FEATURES (Converted from PDF Progress)
# =============================================================================
PROGRESS_STEP_TRACKING=true        # Track progress steps
PROGRESS_EMOJI_ENABLED=true        # Enable progress emojis
PROGRESS_VERBOSE=true              # Verbose progress output
PROGRESS_STEP_NUMBERING=true       # Number progress steps
PROGRESS_TIMING_DETAILED=true      # Detailed timing information
PROGRESS_FILE_OPERATIONS=true      # Track file operations
PROGRESS_IMAGE_OPERATIONS=true     # Track image operations
PROGRESS_TEXT_OPERATIONS=true      # Track text operations

# ERROR HANDLING FEATURES (Converted from PDF Error Handling)
# =============================================================================
ERROR_HANDLING_ENABLED=true        # Enable error handling
ERROR_CONTINUE_ON_FAILURE=true     # Continue on failure
ERROR_SKIP_MISSING_FILES=true      # Skip missing files
ERROR_LOG_DETAILED=true            # Log detailed errors
ERROR_CREATE_PLACEHOLDER=true      # Create placeholder on error
ERROR_NOTIFY_ON_FAILURE=true       # Notify on failure
ERROR_MAX_FAILURES=10              # Maximum failures allowed
ERROR_RETRY_ATTEMPTS=3             # Retry attempts
ERROR_RETRY_DELAY=1.0              # Retry delay
ERROR_FALLBACK_STRATEGY=graceful   # Fallback strategy
ERROR_PLACEHOLDER_TEXT=Content Unavailable  # Placeholder text

# VOLUME CONFIGURATION (Converted from PDF Volume)
# =============================================================================
VOLUME_NUMBER=1                    # Volume number
VOLUME_FORMAT=02d                  # Volume format
QA_PAIRS_PER_VOLUME=100           # Q&A pairs per volume

# OUTPUT DIRECTORY CONFIGURATION (Converted from PDF Output)
# =============================================================================
OUTPUT_DIR=images                  # Images output directory
COVERS_DIR=images/covers           # Cover images directory
TOC_DIR=images/toc                 # Table of contents directory
COMPILATIONS_DIR=images/compilations  # Compilations directory
TEMP_DIR=images/temp               # Temporary files directory
INDIVIDUAL_DIR=images/individual   # Individual Q&A images directory

# TOC CONTENT SETTINGS
# =============================================================================
TOC_MAX_QUESTIONS_PER_CATEGORY=5  # Max questions to show per category in TOC
TOC_QUESTION_PREVIEW_LENGTH=50    # Question preview length in TOC
TOC_SHOW_CATEGORY_COUNTS=true     # Show category counts in TOC
TOC_SHOW_GENERATION_DATE=true     # Show generation date in TOC

# COMPILATION SETTINGS
# =============================================================================
COMPILATION_MAX_IMAGES_PER_PAGE=4 # Max images per compilation page
COMPILATION_LAYOUT_GRID=true      # Use grid layout for compilations
COMPILATION_INCLUDE_CATEGORIES=true  # Include category information in compilations

# PLACEHOLDER SETTINGS
# =============================================================================
PLACEHOLDER_BACKGROUND_COLOR=#f0f0f0  # Placeholder background color
PLACEHOLDER_TEXT_COLOR=#666666        # Placeholder text color
PLACEHOLDER_TEXT=Content Unavailable  # Placeholder text

# TIMING AND PERFORMANCE
# =============================================================================
IMAGE_GENERATION_TIMEOUT=300      # Image generation timeout (seconds)
BATCH_SIZE=5                      # Batch size for image generation
PARALLEL_PROCESSING=false         # Enable parallel processing
RETRY_ATTEMPTS=3                  # Number of retry attempts

# OUTPUT FORMATS
# =============================================================================
OUTPUT_FORMAT=jpg                 # Output image format (jpg, png)
COMPRESSION_QUALITY=95            # Compression quality
INCLUDE_METADATA=true             # Include metadata in images
GENERATE_THUMBNAILS=false         # Generate thumbnail versions

# QUALITY CONTROL
# =============================================================================
QUALITY_CHECK_ENABLED=true        # Enable quality checks
MIN_IMAGE_SIZE=100000             # Minimum image size in bytes
MAX_IMAGE_SIZE=10000000           # Maximum image size in bytes
VALIDATE_IMAGE_INTEGRITY=true     # Validate image integrity

# BACKUP AND RECOVERY
# =============================================================================
BACKUP_ORIGINAL_IMAGES=true       # Backup original images
BACKUP_RETENTION_DAYS=7           # Backup retention period
RECOVERY_MODE=false               # Enable recovery mode

# NOTIFICATIONS
# =============================================================================
NOTIFY_ON_COMPLETION=true         # Notify on completion
NOTIFY_ON_ERROR=true              # Notify on error
NOTIFY_ON_PROGRESS=false          # Notify on progress milestones

# DEBUGGING
# =============================================================================
DEBUG_MODE=false                  # Enable debug mode
VERBOSE_OUTPUT=false              # Enable verbose output
SAVE_INTERMEDIATE_FILES=false     # Save intermediate files

# IMAGE TEXT OVERLAY CONFIGURATION
# =============================================================================
MAX_CHARS_PER_LINE=35            # Max chars per line
MAX_TEXT_LINES_ANSWER=12          # Max text lines for answer
LINE_HEIGHT=72                   # Line height
TEXT_AREA_START=40               # Text area start position
BRAND_TEXT=ASK: Daily Architectural Research  # Brand text
BRAND_X_POSITION=40              # Brand X position
BRAND_Y_OFFSET=100               # Brand Y offset
SEPARATOR_LINE_COLOR=255,255,255,40  # Separator color (RGBA)
SEPARATOR_LINE_WIDTH=1           # Separator width
TEXT_LEFT_MARGIN=40              # Text left margin
SHADOW_OFFSET=2                  # Shadow offset
TEXT_SHADOW_COLOR=0,0,0,100      # Text shadow color (RGBA)
TEXT_COLOR=#F0F0F0               # Text color
SHADOW_COLOR=0,0,0,80            # Shadow color (RGBA)

# FONT CONFIGURATION
# =============================================================================
FONT_FILE_PATH=fonts/arial.ttf   # Font file path
MAIN_FONT_SIZE=32                # Main font size
BRAND_FONT_SIZE=24               # Brand font size
NUMBER_FONT_SIZE=20              # Number font size
NUMBER_X_OFFSET=40               # Number X offset
NUMBER_Y_OFFSET=40               # Number Y offset

# DIRECTORY CONFIGURATION
# =============================================================================
IMAGES_DIR=images                # Images directory
IMAGE_FILENAME_TEMPLATE=ASK-{image_number:02d}-{category}-{image_type}.jpg  # Image filename template
IMAGE_OUTPUT_DIR=images              # IMAGE output directory
LOGS_DIR=logs                    # Logs directory
FONTS_DIR=fonts                  # Fonts directory

# QUESTION GENERATION CONFIGURATION
# =============================================================================
QUESTIONS_PER_category=1          # Questions per category (1 Q&A pair per run)
ENFORCE_category_COUNT=true      # Enforce category count
REQUIRED_categories=architectural_design,construction_technology,design_innovation,engineering_systems,interior_environments,urban_planning,urban_design,digital_technology  # Required categories
QUESTION_MIN_LENGTH=10           # Min question length

# CHAINED QUESTION FLOW CONFIGURATION
# =============================================================================
CHAINED_FLOW_ENABLED=true        # Enable chained flow
CHAINED_FLOW_LENGTH=3            # Chain length
CHAINED_FLOW_OVERLAP=0.3         # Chain overlap
ANSWER_SELECTION_FROM_LOG=true    # Use log.csv answers
ANSWER_SELECTION_LIMIT=5          # Answer selection limit
ANSWER_PREFER_SAME_CATEGORY=true  # Prefer same category

# STYLE GENERATION CONFIGURATION
# =============================================================================
STYLE_GENERATION_ENABLED=true    # Enable style generation
STYLE_CACHE_ENABLED=true         # Enable style cache
STYLE_FALLBACK_ENABLED=true      # Enable style fallback

# RESEARCH EXPLORER CONFIGURATION
# =============================================================================
RESEARCH_EXPLORER_ENABLED=true   # Enable research explorer
RESEARCH_DIMENSIONS_ENABLED=true # Enable dimensions
RESEARCH_INSIGHTS_ENABLED=true   # Enable insights

# DATA MANAGEMENT CONFIGURATION
# =============================================================================
LOG_CSV_FILE=log.csv             # CSV log file
LOG_BACKUP_ENABLED=true          # Enable backup
LOG_BACKUP_RETENTION=7           # Backup retention

# IMAGE GENERATION CONFIGURATION
# =============================================================================
IMAGE_CREATE_INDIVIDUAL_IMAGES=false    # Create individual IMAGEs
IMAGE_CREATE_FINAL_COMPILATION=false  # Create final compilation
IMAGE_CREATE_AUTOMATIC_CATEGORY_COMPILATIONS=false  # Auto category compilations
IMAGE_CREATE_COVER_IMAGE=false        # Create cover image
IMAGE_CREATE_TABLE_OF_CONTENTS=false  # Create TOC
IMAGE_CREATE_SEQUENTIAL_TOC=false     # Create sequential TOC
IMAGE_CREATE_CATEGORY_TOC=false       # Create category TOC
IMAGE_PRESERVE_TEMP_FILES=false       # Preserve temp files

# IMAGE Margin Settings (Standard 1-inch margins)
IMAGE_MARGIN_LEFT=72                  # Left margin (points)
IMAGE_MARGIN_TOP=72                   # Top margin (points)
IMAGE_MARGIN_RIGHT=72                 # Right margin (points)
IMAGE_MARGIN_BOTTOM=72                # Bottom margin (points)

# Typography Configuration
IMAGE_FONT_FAMILY_PRIMARY=Helvetica      # Primary font
IMAGE_FONT_FAMILY_SECONDARY=Times-Roman  # Secondary font
IMAGE_FONT_FAMILY_MONOSPACE=Courier      # Monospace font
IMAGE_FONT_SIZE_TITLE=36                 # Title size
IMAGE_FONT_SIZE_SUBTITLE=18              # Subtitle size
IMAGE_FONT_SIZE_SECTION=16               # Section size
IMAGE_FONT_SIZE_CATEGORY_HEADER=20       # Category header size
IMAGE_FONT_SIZE_ENTRY=14                 # Entry size
IMAGE_FONT_SIZE_QUESTION=12              # Question size
IMAGE_FONT_SIZE_DETAIL=10                # Detail size
IMAGE_FONT_SIZE_FOOTER=10                # Footer size
IMAGE_FONT_SIZE_CAPTION=8                # Caption size
IMAGE_FONT_SIZE_COVER_TITLE=48           # Cover title size
IMAGE_FONT_SIZE_COVER_SUBTITLE=24        # Cover subtitle size
IMAGE_FONT_SIZE_COVER_BRAND=18           # Cover brand size
IMAGE_COLOR_PRIMARY=#000000              # Primary color
IMAGE_COLOR_SECONDARY=#2C3E50            # Secondary color
IMAGE_COLOR_ACCENT=#E74C3C               # Accent color
IMAGE_COLOR_CATEGORY_HEADER=#8E44AD      # Category header color
IMAGE_COLOR_MUTED=#7F8C8D                # Muted color
IMAGE_COLOR_BRAND=#34495E                # Brand color

# Line and Character Spacing Configuration
IMAGE_LINE_SPACING_SINGLE=1.0            # Single spacing
IMAGE_LINE_SPACING_ONE_HALF=1.5          # 1.5 spacing
IMAGE_LINE_SPACING_DOUBLE=2.0            # Double spacing
IMAGE_CHAR_SPACING_NORMAL=0              # Normal char spacing
IMAGE_CHAR_SPACING_TIGHT=-0.5            # Tight char spacing
IMAGE_CHAR_SPACING_WIDE=1.0              # Wide char spacing

# Spacing Configuration
IMAGE_TITLE_TO_SUBTITLE_SPACING=20       # Title-subtitle spacing
IMAGE_SUBTITLE_TO_SECTION_SPACING=30     # Subtitle-section spacing
IMAGE_SECTION_TO_CONTENT_SPACING=40      # Section-content spacing
IMAGE_ENTRY_SPACING=15                   # Entry spacing
IMAGE_CATEGORY_SPACING=20                # Category spacing
IMAGE_SECTION_SPACING=25                 # Section spacing

# Question Display Configuration
IMAGE_TOC_SHOW_FULL_QUESTIONS=true    # Show full questions
IMAGE_TOC_QUESTION_PREVIEW_LENGTH=0   # Question preview length
LOG_QUESTION_DISPLAY_LENGTH=100     # Log display length

# MODE-SPECIFIC CONFIGURATION (Updated for 10 Q&A pairs each)
# =============================================================================
# Hybrid Mode Configuration
HYBRID_THEME_COUNT=5                    # Number of themes to explore (5 themes × 2 chains = 10 Q&A pairs)
HYBRID_CHAIN_LENGTH=2                   # Questions per chain (5 themes × 2 chains = 10 Q&A pairs)

# Cross-Disciplinary Mode Configuration
CROSS_DISCIPLINARY_THEME_COUNT=10       # Number of themes (10 themes = 10 Q&A pairs)

# Chained Mode Configuration
CHAIN_LENGTH=5                          # Questions per chain (2 categories × 5 chains = 10 Q&A pairs)
CATEGORIES_TO_GENERATE=architectural_design,construction_technology  # Categories for chained mode

# Sequential Numbering Configuration
IMAGE_NUMBER_INCREMENT=2             # Image number increment
IMAGE_NUMBER_START=1                 # Starting image number
IMAGE_NUMBER_FORMAT=02d              # Image number format
IMAGE_NUMBER_INCLUDE_IN_QA_PAIRS=true # Include in Q&A pairs
VOLUME_NUMBER_FORMAT=02d             # Volume number format

# TOC Grouping Configuration
IMAGE_TOC_CATEGORY_GROUPING=true     # Group by category
IMAGE_TOC_GROUP_UNKNOWN_CATEGORIES=true  # Group unknown categories
IMAGE_TOC_UNKNOWN_CATEGORY_NAME=Uncategorized  # Unknown category name
IMAGE_TOC_SORT_CATEGORIES_ALPHABETICALLY=true  # Sort alphabetically
IMAGE_TOC_IMAGE_NUMBER_SPACE=50       # Image number space

# TOC Content Configuration
IMAGE_TOC_MAIN_TITLE_TEMPLATE=ASK: Daily Architectural Research - Volume {volume_number}  # Main title
IMAGE_TOC_SEQUENTIAL_SUBTITLE=Sequential Table of Contents  # Sequential subtitle
IMAGE_TOC_CATEGORY_SUBTITLE=Category Index  # Category subtitle
IMAGE_TOC_SEQUENTIAL_SECTION_TITLE=Sequential Order  # Sequential section
IMAGE_TOC_CATEGORY_INFO_TEMPLATE={count} Q&A Pairs • Sequential Order  # Category info
IMAGE_TOC_TOTAL_COUNT_TEMPLATE=Total Q&A Pairs: {count}  # Total count
IMAGE_TOC_CATEGORY_OVERVIEW_TEMPLATE=Category Overview and Research Insights  # Category overview
IMAGE_TOC_RESEARCH_INSIGHTS_TEMPLATE=Research Insights: {insights}...  # Research insights
IMAGE_TOC_DATE_TEMPLATE=Generated: {date}  # Date template
IMAGE_TOC_TITLE_TEMPLATE=Table of Contents  # TOC title
IMAGE_TOC_SUBTITLE_TEMPLATE=ASK: Daily Architectural Research - Volume {volume_number}  # TOC subtitle
IMAGE_TOC_IMAGE_NUMBER_TEMPLATE={image_number}  # Image number template
IMAGE_TOC_SUMMARY_TEMPLATE=Total Q&A Pairs: {qa_count} | Categories: {category_count}  # Summary

# Individual IMAGE Configuration
IMAGE_INDIVIDUAL_FILENAME_TEMPLATE=ASK_{image_number}_{category}.jpg  # Individual filename
IMAGE_INDIVIDUAL_FALLBACK_TEMPLATE=ASK_QA_{category}.jpg  # Fallback filename
IMAGE_FOOTER_BRAND_TEXT=ASK: Daily Architectural Research  # Footer brand
IMAGE_FOOTER_IMAGE_NUMBER_TEMPLATE=ASK-{image_number}  # Footer image number
IMAGE_TEXT_WRAP_WIDTH_ANSWER=60  # Answer wrap width
IMAGE_TEXT_WRAP_WIDTH_OVERLAY=50  # Overlay wrap width
IMAGE_FOOTER_BACKGROUND_COLOR=0.1,0.1,0.1  # Footer background
IMAGE_DEFAULT_PAGE_WIDTH=612  # Default page width
IMAGE_DEFAULT_PAGE_HEIGHT=792  # Default page height
IMAGE_FALLBACK_PAGE_WIDTH=612  # Fallback page width
IMAGE_FALLBACK_PAGE_HEIGHT=792  # Fallback page height

# IMAGE Page Configuration
IMAGE_PAGE_WIDTH=1072              # Page width
IMAGE_PAGE_HEIGHT=1792             # Page height
IMAGE_PAGE_BREAK_BUFFER=100        # Page break buffer
IMAGE_MAX_TEXT_LINES=8             # Max text lines
IMAGE_TEXT_WRAP_WIDTH_ANSWER=60     # Answer wrap width
IMAGE_TEXT_WRAP_WIDTH_OVERLAY=50   # Overlay wrap width
IMAGE_FOOTER_HEIGHT=80             # Footer height
IMAGE_FONT_SIZE_FOOTER=14          # Footer font size
IMAGE_FOOTER_BRAND_X=40            # Footer brand X
IMAGE_FOOTER_BRAND_Y_OFFSET=20     # Footer brand Y offset
IMAGE_FOOTER_CATEGORY_Y_OFFSET=20  # Footer category Y offset
IMAGE_FOOTER_IMAGE_X_OFFSET=40     # Footer image X offset
IMAGE_FOOTER_IMAGE_Y_OFFSET=20     # Footer image Y offset
IMAGE_TEXT_COLOR=#FFFFFF           # Text color
IMAGE_LINE_SPACING=30              # Line spacing
IMAGE_MARGIN_BOTTOM=200            # Bottom margin

# LOGGING CONFIGURATION
# =============================================================================
LOG_DIR=logs                   # Log directory
LOG_FILE=execution.log         # Log filename
LOG_SEPARATOR_LENGTH=60        # Separator length
LOG_QUESTION_DISPLAY_LENGTH=100     # Question display length

# Individual IMAGE Logging Configuration
IMAGE_LOG_LEVEL=INFO             # Log level
IMAGE_LOG_SUCCESS_MESSAGES=true  # Log success
IMAGE_LOG_ERROR_MESSAGES=true    # Log errors
IMAGE_LOG_PROGRESS_MESSAGES=true # Log progress
IMAGE_LOG_DETAILED_ERRORS=true   # Log detailed errors
IMAGE_LOG_TIMING=true           # Log timing

# Progress Tracking Configuration
IMAGE_PROGRESS_STEP_TRACKING=true    # Step tracking
IMAGE_PROGRESS_EMOJI_ENABLED=true    # Emoji enabled
IMAGE_PROGRESS_VERBOSE=true          # Verbose progress
IMAGE_PROGRESS_STEP_NUMBERING=true   # Step numbering
IMAGE_PROGRESS_TIMING_DETAILED=true  # Detailed timing
IMAGE_PROGRESS_FILE_OPERATIONS=true  # File operations
IMAGE_PROGRESS_IMAGE_OPERATIONS=true # Image operations
IMAGE_PROGRESS_TEXT_OPERATIONS=true  # Text operations

# Error Handling Configuration
IMAGE_ERROR_HANDLING_ENABLED=true    # Error handling
IMAGE_ERROR_CONTINUE_ON_FAILURE=true # Continue on failure
IMAGE_ERROR_SKIP_MISSING_FILES=true  # Skip missing files
IMAGE_ERROR_RETRY_ATTEMPTS=3         # Retry attempts
IMAGE_ERROR_RETRY_DELAY=1.0          # Retry delay
IMAGE_ERROR_FALLBACK_STRATEGY=graceful # Fallback strategy
IMAGE_ERROR_LOG_DETAILED=true        # Log detailed errors
IMAGE_ERROR_CREATE_PLACEHOLDER=true  # Create placeholders
IMAGE_ERROR_PLACEHOLDER_TEXT=Content Unavailable # Placeholder text
IMAGE_ERROR_MAX_FAILURES=10          # Max failures
IMAGE_ERROR_NOTIFY_ON_FAILURE=true   # Notify on failure

# COVER GENERATION CONFIGURATION
# =============================================================================
IMAGE_COVER_PROMPT=Professional architectural research compilation cover with modern design elements, typography, and visual hierarchy  # Cover prompt
IMAGE_COVER_TITLE=ASK: Daily Architectural Research  # Cover title
IMAGE_COVER_TEXT_COLOR=#000000  # Cover text color
IMAGE_COVER_BRAND_COLOR=#2C3E50  # Cover brand color

# TOC BACKGROUND CONFIGURATION
# =============================================================================
IMAGE_TOC_BACKGROUND_PROMPT=true  # TOC background prompt

# VOLUME CONFIGURATION
# =============================================================================
IMAGE_VOLUME_NUMBER=1              # Volume number
IMAGE_VOLUME_FORMAT=02d            # Volume format
QA_PAIRS_PER_VOLUME=100            # Q&A pairs per volume before incrementing

# SYSTEM PROMPTS CONFIGURATION
# =============================================================================
ANSWER_SYSTEM_PROMPT=You are an expert architectural researcher and educator analyzing architectural concepts and imagery.  # Answer prompt
STYLE_SYSTEM_PROMPT=You are an expert architectural style consultant. Analyze the given architectural question and suggest the most appropriate architectural style(s) that would best represent or complement the concept.  # Style prompt
RESEARCH_EXPLORER_CATEGORY_PROMPT=You are an expert architectural category consultant. Analyze the given architectural question and category to suggest the most appropriate categories that would best address or explore the concept.  # Category prompt
RESEARCH_EXPLORER_QUESTION_PROMPT=You are an expert architectural category consultant. Analyze the given architectural question and suggest the most appropriate architectural category(s) that would best address or explore the concept.  # Question prompt

# TEST CONFIGURATION
# =============================================================================
TEST_QUESTION=How can we design sustainable buildings that respond to climate change?  # Test question

# OUTPUT DIRECTORY CONFIGURATION
# =============================================================================
OUTPUT_DIR=images  # Output directory

# Cross-Disciplinary Generation Settings
CROSS_DISCIPLINARY_ENABLED=true
CROSS_DISCIPLINARY_FREQUENCY=0.3
CROSS_DISCIPLINARY_THEME_FREQUENCY=0.2
CROSS_DISCIPLINARY_SYNTHESIS_FREQUENCY=0.1