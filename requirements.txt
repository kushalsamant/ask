Pillow>=10.0.0  # Image processing and manipulation
PyMuPDF>=1.23.0  # PDF processing and manipulation
aiohttp>=3.9.0  # Asynchronous HTTP client/server
beautifulsoup4>=4.12.0  # HTML/XML parsing (legacy, not used)
lxml>=4.9.0  # XML/HTML processing (legacy, not used)
python-dotenv>=1.0.0  # Environment variable management
reportlab>=4.0.0  # PDF generation
requests>=2.31.0  # HTTP library for API calls
tqdm>=4.66.0  # Progress bar for command line
groq>=0.4.0  # Groq API client (legacy, not used)
replicate>=0.22.0  # Replicate API client (legacy, not used)
openai>=1.0.0  # OpenAI API client (legacy, not used)